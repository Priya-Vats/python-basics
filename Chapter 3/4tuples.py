#tuple = (2, 4, 5, 9)
#print(tuple[0])
#print(tuple[1])
#tuple[1] = 7 #value can not be changed . hence, it is immutable

#tuple = (1, ) #it is compulsory to add ',' with a single element at the end otherwise it will be considered ad int, float or string
#print(tuple)
#print(type(tuple))

tuple = (1, 2, 3, 4) #with multiple elements, we do not need to add ',' at the end.
print(tuple[1:4])